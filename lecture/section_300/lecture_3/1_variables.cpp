// CSCI 1300, Fall 2023
// Supriya Naidu
// Lecture 3: working with variables - recap

#include<iostream>

using namespace std;

int main()
{
    int x = 0; // define and initialize variable; recommended mwthod when working with variables
    x = 10; // can re-assign value to an existing variable
    x = 100;
    x = -90; // can re-assign value multiple times
    int y; // garbage value
    // char x; // cannot re-use a variable name in a program
    // bool x;
    cout << y << endl;

    return 0;
}